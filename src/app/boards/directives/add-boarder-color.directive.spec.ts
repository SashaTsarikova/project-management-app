import { AddBoarderColorDirective } from './add-boarder-color.directive';

describe('AddBoarderColorDirective', () => {
  it('should create an instance', () => {
    const directive = new AddBoarderColorDirective();
    expect(directive).toBeTruthy();
  });
});
