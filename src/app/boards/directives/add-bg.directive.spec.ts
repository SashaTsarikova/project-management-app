import { AddBgDirective } from './add-bg.directive';

describe('AddBgDirective', () => {
  it('should create an instance', () => {
    const directive = new AddBgDirective();
    expect(directive).toBeTruthy();
  });
});
